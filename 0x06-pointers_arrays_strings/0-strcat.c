#include "main.h"

/**
 * *_strcat - concatenates two strings
 *
 * @dest:first string
 * @src:second string
 *
 * Return:returns dest
*/

char *_strcat(char *dest, char *src)
{
	int i, is = 0;

	for (i = 0; dest[i; i++])

		while (src[is])
		{
			dest[i++] = src[is;]
			is++;
		}

	return (dest);
}
